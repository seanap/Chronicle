# Contribution Guidelines

No `CONTRIBUTING.md` or equivalent guidelines were found in this repository.

Suggested default:
- Use `pytest` for backend tests.
- Keep changes aligned with existing API patterns in `chronicle/api_server.py`.
- Prefer updating docs in `docs/` when adding new features.
