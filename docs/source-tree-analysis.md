# Source Tree Analysis

```
Auto-Stat-Description/
├── chronicle/                 # Python backend (Flask API, worker, pipeline)
│   ├── api_server.py          # HTTP routes + HTML pages
│   ├── worker.py              # Background processing loop
│   ├── activity_pipeline.py   # Orchestrates activity processing
│   ├── plan_data.py           # Plan sheet payloads
│   ├── dashboard_data.py      # Dashboard/heatmap payloads
│   ├── template_*             # Template profiles + repository + rendering
│   └── stat_modules/          # Stats computations (misery index, intervals, etc.)
├── templates/                 # Server-rendered HTML pages
├── static/                    # Frontend JS/CSS assets
├── android/                   # Android widget companion app
│   └── app/src/main/          # Kotlin sources + res/ widgets
├── tests/                     # Python test suite
├── docs/                      # Documentation (API docs, reports, generated docs)
├── local/                     # Gitignored planning docs (user-provided context)
├── state/                     # Runtime state, templates, and DB (local)
├── data/                      # Seed and template data
├── Dockerfile                 # Container build
├── docker-compose.yml         # Local stack
└── requirements.txt           # Python dependencies
```

## Notes
- Build artifacts (e.g., `android/app/build/`, `.gradle/`, `.venv/`) are excluded from analysis.
- The backend is a monolithic Flask app with server-rendered HTML and local JSON APIs.
- Android app is a standalone widget companion consuming `/plan/today.json`.

---
_Generated by document-project workflow_ 
